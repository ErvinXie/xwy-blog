# 2026-03-13

## Session 1

- 将 meta-agent 添加为 git submodule，并用 init.sh 完成初始化
- 更新 meta-agent submodule 到最新版本（6da6894）
- 按照 meta-agent 方法论初始化本仓库的上下文体结构：
  - 创建 `daily-notes/` 目录，开始记录工作日志
  - 创建 `doc/` 目录，用于存放项目知识快照
  - 创建 `handoff/` 目录，用于 agent 间任务交接
  - 更新 `CLAUDE.md`，加入项目上下文信息

## Session 2

- 写了博文 `content/posts/agent-native.mdx`，主题是 Agent Native 方法论（偏理论）
- 文章结构：上下文瓶颈 → agent = 上下文体 + 运行时 → 三个核心组件（daily notes / doc / handoff） → 信息流动 → 内部协作与外部协作 → 开放问题
- 迭代过程中的关键决策：
  - SOP 是 doc 的特殊形式，不是并列的独立组件
  - handoff 是核心组件，和 daily notes、doc 平级
  - "协作的本质是传递上下文"限定为内部协作，外部协作是 context-small 的
  - context-free 是相对概念，取决于交互双方所需的上下文量
- 用 humanizer 做了一轮 AI 痕迹清理：减少 em dash、负面平行结构、rule-of-three、slogan 式结尾等
- 已 commit 并 push
