# 2026-04-01

## Session 1

- 为博客文章页接入 `giscus` 评论系统，新增 `src/components/blog/Comments.tsx`，并挂载到 `src/app/blog/[slug]/page.tsx`
- 评论系统采用 GitHub Discussions 作为存储，不引入站内数据库或用户系统，更适合当前 `Next.js + MDX + Vercel` 的轻量博客结构
- 补充了评论相关配置说明和示例环境变量：
  - 更新 `README.md`
  - 新增 `.env.example`
- 初版实现后完成一次提交并推送：
  - `d3778b4` `Add giscus comments to blog posts`

## Session 2

- 根据 giscus 配置页生成的脚本，对齐运行时配置：
  - `data-strict="0"`
  - `data-input-position="bottom"`
  - 默认主题改为 `preferred_color_scheme`
- 完成一次同步配置的提交并推送：
  - `8fe075a` `Align giscus settings with production`
- 协助确认 Vercel 侧需要配置的环境变量，并说明修改环境变量后需要重新部署
- 线上检查结果：
  - `xieweiyu.me` 会跳转到 `www.xieweiyu.me`
  - 文章页已出现评论区容器，说明评论功能代码已进入生产环境

## Session 3

- 用户反馈评论区风格与现有博客视觉不一致
- 为 `giscus` 增加站内竹色主题和宿主卡片样式：
  - 新增 `public/giscus-bamboo.css`
  - 更新 `src/components/blog/Comments.tsx`
  - 更新 `src/app/globals.css`
- 自定义主题策略：
  - 当 `NEXT_PUBLIC_GISCUS_THEME` 为 `preferred_color_scheme` 或 `light` 时，自动回退为站内自定义主题文件
  - 仍保留通过环境变量覆盖为其他 giscus 主题或外部主题 URL 的能力
- 样式改版后重新构建验证通过，并完成提交推送：
  - `987e29a` `Restyle giscus comments to match blog`
- 推送后立即检查线上，暂时仍看到旧版评论区 HTML，推测为 Vercel 尚未切换到最新部署或仍在发布过程中
